#!/usr/bin/env python
from setuptools import setup

setup(
    name='splendor',
    description='splendor core code',
    packages=[
    ],
    install_requires=[
        'py_cui'
    ],
    dependency_links=[
    ],
    extras_require={
        'dev': [
        ]
    }
)
