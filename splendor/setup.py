#!/usr/bin/env python
from setuptools import setup

setup(
    name='splendor',
    description='splendor core code',
    packages=[
        'py_cui'
    ],
    install_requires=[
    ],
    dependency_links=[
    ],
    extras_require={
        'dev': [
        ]
    }
)
